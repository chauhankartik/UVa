#include<bits/stdc++.h>
using namespace std;
#define int long long int

int32_t main() {

    int t; cin >> t;
    while(t--) {

        int n, m; cin >> n >> m;
        vector<string> adj(n);
        for(int i = 0; i < n; i++) {
            string s;
            cin >> s;
            adj[i] = s;
        }

        
    }    
    return 0;
}