#include<bits/stdc++.h>
using namespace std;

struct ListNode {
    int val;
    ListNode* next;
};

class Solution {
public:
    ListNode* partition(ListNode* head, int x) {
        ListNode *moveback, *moveforward;  
        while(moveback && moveforward) {
            
        }
    }
};

int main() {
    //string s = 1 + "";
    //cout << s << endl;
    string s = to_string(1);
    cout << s << endl;
}