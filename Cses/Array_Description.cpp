#include<bits/stdc++.h>
using namespace std;

int main() {

        int n, m; cin >> n >> m;
        int mod = 1e9+7;
        vector<int> vec(n+1, 0);
        for(int i = 1; i <= n; i++) cin >> vec[i];

        vector<vector<int>> dp(n+1, vector<int>(m+1, 0));

        /*
                dp[i][j] : 
        */

        return 0;
}