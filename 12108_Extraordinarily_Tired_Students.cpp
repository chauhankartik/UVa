#include<bits/stdc++.h>
using namespace std;

#define int long long int

int32_t main () {

        int n;
        while((cin >> n) && n != 0)
        {
                int res = -1;
                int a, b, c;
                while(n--) {
                        cin >> a >> b >> c;
                        queue<int> q;
                        q.push(a+b-c);
                        
                }
        }
        return 0;
}