#include<bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
    int n, c, b; cin >> n >> b >> c;
    vector<int> v(n);
    for(auto &c : v) cin >> c;
    int res = 0;
    for(int i = 0; i < n; i++) {
        
    }
    cout << res << endl;
    return 0;
}
