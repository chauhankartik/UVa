#include<bits/stdc++.h>
using namespace std;
#define int long long int

int32_t main() {
    int t; scanf("%lld", &t); 
    while(t--) {
        int n, x, k;
        scanf("%lld %lld %lld", &n, &x, &k);
        
    }
    return 0;
}