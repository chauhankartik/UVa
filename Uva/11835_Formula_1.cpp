#include<bits/stdc++.h>
using namespace std;
#define int long long int
#define sc(n) scanf("%lld", &n)

int32_t main() {

        int g, p;
        while(scanf("%lld %lld", &g, &p) == 2 && (g&&p)) {
                vector<int> results(p);
                for(int i = 0; i < p; i++)
                        sc(results[i]);
                        
        }
        return 0;
}