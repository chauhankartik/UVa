#include<bits/stdc++.h>
using namespace std;
#define int long long int

int32_t main() {
        int t; cin >> t;
        while(t--) {
                string s; getline(cin, s);
                int max_len = -1;
                int res = -1;
                for(int i = 0; i < s.length(); i++) {
                        int current = 0;
                        if(s[i] >= '0' && s[i] <= '9') {
                                
                        }
                }
        }
        return 0;
}